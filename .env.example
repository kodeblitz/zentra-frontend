# Copiar a .env y ajustar si hace falta.
# docker compose up -d leerá .env automáticamente.

# URL del API que verá el navegador (frontend en Docker).
# Debe ser la URL con la que accedes al backend desde tu máquina.
ZENTRA_API_URL=http://localhost:8080
